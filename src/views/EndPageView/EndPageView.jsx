import React from 'react';
import s from './EndPageView.module.scss';

export default function EndPageView() {
  return (
    <section className={s.section}>
      <div className={s.container}>
        <h2 className={s.title}>EndPageView</h2>
      </div>
    </section>
  );
}
